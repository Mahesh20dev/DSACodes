import java.util.Scanner;
public class Pattern {
    public static void  main(String[] args) {
         Scanner sc = new Scanner(System.in);

        System.out.println("Enter two no");

        int a = sc.nextInt();
        int b = sc.nextInt();
        int g = 0;

        for(int i=1; i<b; i++) {
            if(a%i==0 && b%i==0) {
               // System.out.println(i); // if u want to not print every divisor
                                       // just comment off
                g = i;
            }

        }
        System.out.println("GCD is "+ g);
    }

}

output:

Enter two no
100
20
GCD is 10
